<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sp.app.mapper.AttendanceMapper">
	
	<select id="loginMember" parameterType="map" resultType="com.sp.app.model.Member">
		SELECT m1.memberId, m1.userId, password, name, nickname, email, userLevel 
		FROM member1 m1
		LEFT OUTER JOIN member2 m2 ON m1.memberId = m2.memberId
		WHERE m1.userId = #{userId} AND password = #{password}
	</select>

	
</mapper>